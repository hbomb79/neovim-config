### Language Setup

This directory contains the language specifications for this Neovim configuration. Each file in this directory
is responsible for defining the languages plugins, formatters, linters, etc. This is done by registering the
language with the language registry (see [./init.lua](./init.lua))
