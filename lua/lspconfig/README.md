### LSP Setup

This file contains LSP setup hooks for particular languages. It is lazy-loaded when the a file of matching filetype is opened.
Refer to the root `init.lua` for the full list of configured languages; only languages with large configuration which would
be distasteful to inline are found in this directory.
